# v0.4.A — TTS 음성 응답 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 채팅 어시스턴트 응답을 Android TextToSpeech로 낭독한다(상단바 토글, 완료 시 낭독).

**Architecture:** `:local`에 `TtsSpeaker`(TextToSpeech 래퍼, @Singleton) 추가, `ChatViewModel`이 주입받아 스트리밍 완료 시 낭독하고 토글/중단을 관리, `ChatScreen` 상단바에 on/off 토글.

**Tech Stack:** Android `TextToSpeech`, Hilt, Compose, Coroutines.

**선행:** v0.4 채팅(PR #11, feature/v0.4-plan). 본 브랜치 `feature/v0.4a-tts`는 그 위에 쌓임. spec: `docs/superpowers/specs/2026-06-08-v0.4a-tts-voice-response-design.md`.

---

## File Structure

생성:
- `local/src/main/java/com/just/assistant/local/audio/TtsSpeaker.kt` (인터페이스 + impl)

수정:
- `local/src/main/java/com/just/assistant/local/di/LocalModule.kt` (@Provides)
- `feature_chat/build.gradle.kts` (`:local` 의존)
- `feature_chat/src/main/java/com/just/feature/chat/chat/ChatViewModel.kt`
- `feature_chat/src/test/java/com/just/feature/chat/ChatViewModelTest.kt`
- `feature_chat/src/main/java/com/just/feature/chat/chat/ChatScreen.kt`
- `feature_chat/src/main/res/values/strings.xml`

---

## Task 1: `:local` TtsSpeaker

**Files:**
- Create: `local/src/main/java/com/just/assistant/local/audio/TtsSpeaker.kt`
- Modify: `local/src/main/java/com/just/assistant/local/di/LocalModule.kt`

- [ ] **Step 1: TtsSpeaker 인터페이스 + 구현**

`TtsSpeaker.kt`:
```kotlin
package com.just.assistant.local.audio

import android.content.Context
import android.speech.tts.TextToSpeech
import dagger.hilt.android.qualifiers.ApplicationContext
import java.util.Locale
import javax.inject.Inject
import javax.inject.Singleton

interface TtsSpeaker {
    /** [text]를 낭독. 이전 발화는 끊고 새로 시작. 미초기화/빈 문자열이면 no-op. */
    fun speak(text: String)

    /** 현재 낭독 중단. */
    fun stop()
}

@Singleton
class TtsSpeakerImpl
    @Inject
    constructor(
        @ApplicationContext context: Context,
    ) : TtsSpeaker {
        @Volatile private var ready = false
        private lateinit var tts: TextToSpeech

        init {
            tts =
                TextToSpeech(context) { status ->
                    if (status == TextToSpeech.SUCCESS) {
                        tts.language = Locale.getDefault()
                        ready = true
                    }
                }
        }

        override fun speak(text: String) {
            if (!ready || text.isBlank()) return
            tts.speak(text, TextToSpeech.QUEUE_FLUSH, null, "chat")
        }

        override fun stop() {
            if (ready) tts.stop()
        }
    }
```

- [ ] **Step 2: LocalModule @Provides**

`LocalModule.kt`(object + @Provides, 확인됨)에 `provideDatabase` 등 옆에 추가:
```kotlin
    @Provides
    @Singleton
    fun provideTtsSpeaker(impl: TtsSpeakerImpl): TtsSpeaker = impl
```
import: `com.just.assistant.local.audio.TtsSpeaker`, `com.just.assistant.local.audio.TtsSpeakerImpl`. (`@Provides`/`@Singleton` 이미 import됨.) `TtsSpeakerImpl`은 `@Inject constructor`라 Hilt가 직접 생성.

- [ ] **Step 3: 컴파일**

Run: `./gradlew :local:compileDebugKotlin`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 4: Commit**

```bash
git add local/src/main/java/com/just/assistant/local/audio/TtsSpeaker.kt local/src/main/java/com/just/assistant/local/di/LocalModule.kt
git commit -m "feat(local): TtsSpeaker (TextToSpeech 래퍼) + Hilt @Provides"
```

---

## Task 2: `ChatViewModel` TTS 통합

**Files:**
- Modify: `feature_chat/build.gradle.kts`
- Modify: `feature_chat/src/main/java/com/just/feature/chat/chat/ChatViewModel.kt`
- Modify: `feature_chat/src/test/java/com/just/feature/chat/ChatViewModelTest.kt`

- [ ] **Step 1: feature_chat가 :local 의존**

`feature_chat/build.gradle.kts` dependencies에 추가(`implementation(project(":ai"))` 옆):
```kotlin
    implementation(project(":local"))
```

- [ ] **Step 2: 테스트 갱신 (TDD) — Fake TtsSpeaker + 새 테스트**

`ChatViewModelTest.kt`에 Fake 추가(다른 fake들 근처):
```kotlin
    private class FakeTts : com.just.assistant.local.audio.TtsSpeaker {
        var spokenText: String? = null
        var speakCount = 0
        var stopCount = 0
        override fun speak(text: String) { speakCount++; spokenText = text }
        override fun stop() { stopCount++ }
    }
```

기존 4개 `ChatViewModel(...)` 생성 호출에 두 번째 인자로 `FakeTts()`를 추가한다. 단, speak 검증 테스트는 fake 참조가 필요하므로 그 테스트들은 `val tts = FakeTts()`로 만들어 전달한다. 예: 기존 `ChatViewModel(FakeStart(session))` → `ChatViewModel(FakeStart(session), FakeTts())`.

새 테스트 추가:
```kotlin
    @Test
    fun speaks_final_text_when_tts_enabled_and_stream_completes() =
        runTest {
            val session = FakeSession(listOf("안", "녕"))
            val tts = FakeTts()
            val vm = ChatViewModel(FakeStart(session), tts)
            vm.onToggleTts() // enable
            vm.onInputChanged("hi")
            vm.onSend()
            advanceUntilIdle()
            assertEquals(1, tts.speakCount)
            assertEquals("안녕", tts.spokenText)
        }

    @Test
    fun does_not_speak_when_tts_disabled() =
        runTest {
            val tts = FakeTts()
            val vm = ChatViewModel(FakeStart(FakeSession(listOf("a"))), tts)
            vm.onInputChanged("hi")
            vm.onSend()
            advanceUntilIdle()
            assertEquals(0, tts.speakCount)
        }

    @Test
    fun onSend_stops_previous_speech() =
        runTest {
            val tts = FakeTts()
            val vm = ChatViewModel(FakeStart(FakeSession(listOf("a"))), tts)
            vm.onInputChanged("hi")
            vm.onSend()
            advanceUntilIdle()
            assertEquals(true, tts.stopCount >= 1)
        }

    @Test
    fun toggle_off_stops_speech() =
        runTest {
            val tts = FakeTts()
            val vm = ChatViewModel(FakeStart(FakeSession(listOf("a"))), tts)
            vm.onToggleTts() // on
            vm.onToggleTts() // off → stop
            assertEquals(true, tts.stopCount >= 1)
            assertEquals(false, vm.state.first().ttsEnabled)
        }
```
> `vm.state.first()` 사용 시 import `kotlinx.coroutines.flow.first` 가 이미 있는지 확인(없으면 추가).

Run: `./gradlew :feature_chat:testDebugUnitTest` → 컴파일 실패(ChatViewModel 2-인자 생성자/ onToggleTts/ ttsEnabled 미존재) 확인.

- [ ] **Step 3: ChatViewModel 구현 변경**

import 추가:
```kotlin
import com.just.assistant.local.audio.TtsSpeaker
```

`ChatState`에 필드 추가(`modelReady` 아래):
```kotlin
    val ttsEnabled: Boolean = false,
```

생성자에 `tts` 추가:
```kotlin
    constructor(
        private val startChatSession: StartChatSessionUseCase,
        private val tts: TtsSpeaker,
    ) : ViewModel() {
```

`onSend()`의 `if (s == null) {...}` 블록 **다음**, 메시지 추가 `_state.update` **이전**에 이전 낭독 중단 추가:
```kotlin
                tts.stop()
                _state.update {
                    it.copy(
                        messages = it.messages +
                        // ... 기존 그대로 ...
                    )
                }
```

`streamJob`의 try 블록에서 `collect { ... }` **직후**(catch 이전)에 낭독 추가:
```kotlin
                        try {
                            s.send(text).collect { delta ->
                                _state.update { st ->
                                    val msgs = st.messages.toMutableList()
                                    val last = msgs.lastIndex
                                    msgs[last] = msgs[last].copy(text = msgs[last].text + delta)
                                    st.copy(messages = msgs)
                                }
                            }
                            if (_state.value.ttsEnabled) {
                                val finalText = _state.value.messages.lastOrNull()?.text.orEmpty()
                                if (finalText.isNotBlank()) tts.speak(finalText)
                            }
                        } catch (t: Throwable) {
                            // ... 기존 그대로 ...
```

`onCancel()`에 stop 추가:
```kotlin
        fun onCancel() {
            streamJob?.cancel()
            tts.stop()
            _state.update { it.copy(isStreaming = false) }
        }
```

`onResetConversation()`에 stop 추가:
```kotlin
        fun onResetConversation() {
            streamJob?.cancel()
            tts.stop()
            session?.close()
            session = null
            _state.update { ChatState() }
        }
```

`onCleared()`에 stop 추가:
```kotlin
        override fun onCleared() {
            tts.stop()
            session?.close()
        }
```

`onToggleTts()` 추가(예: `onCancel` 위):
```kotlin
        fun onToggleTts() {
            val newValue = !_state.value.ttsEnabled
            if (!newValue) tts.stop()
            _state.update { it.copy(ttsEnabled = newValue) }
        }
```

- [ ] **Step 4: 테스트 통과**

Run: `./gradlew :feature_chat:testDebugUnitTest`
Expected: PASS (기존 4 + 신규 4 = 8).

- [ ] **Step 5: Commit**

```bash
git add feature_chat/build.gradle.kts feature_chat/src/main/java/com/just/feature/chat/chat/ChatViewModel.kt feature_chat/src/test/java/com/just/feature/chat/ChatViewModelTest.kt
git commit -m "feat(feature_chat): ChatViewModel TTS 낭독(완료 시) + 토글/중단 + 테스트"
```

---

## Task 3: `ChatScreen` 음성 토글 + strings

**Files:**
- Modify: `feature_chat/src/main/java/com/just/feature/chat/chat/ChatScreen.kt`
- Modify: `feature_chat/src/main/res/values/strings.xml`

- [ ] **Step 1: strings**

`feature_chat/src/main/res/values/strings.xml`에 추가:
```xml
    <string name="chat_tts_on">음성 끄기</string>
    <string name="chat_tts_off">음성 켜기</string>
```

- [ ] **Step 2: 상단바 토글 버튼**

`ChatScreen.kt`의 `TopAppBar(... actions = { ... })`에서 기존 "새 대화" `TextButton` **앞**에 음성 토글 추가:
```kotlin
                actions = {
                    TextButton(onClick = viewModel::onToggleTts) {
                        Text(
                            stringResource(
                                if (state.ttsEnabled) R.string.chat_tts_on else R.string.chat_tts_off,
                            ),
                        )
                    }
                    TextButton(onClick = viewModel::onResetConversation) {
                        Text(stringResource(R.string.chat_new))
                    }
                },
```
(`state`는 이미 `collectAsStateWithLifecycle()`로 존재. `TextButton`/`stringResource`/`R` 이미 import됨.)

- [ ] **Step 3: 빌드**

Run: `./gradlew :feature_chat:compileDebugKotlin :feature_chat:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 4: Commit**

```bash
git add feature_chat/src/main/java/com/just/feature/chat/chat/ChatScreen.kt feature_chat/src/main/res/values/strings.xml
git commit -m "feat(feature_chat): 채팅 상단바 음성 출력 토글"
```

---

## Task 4: 전체 빌드 + 디바이스 smoke + memory + PR

**Files:**
- Modify: `/Users/kichan/.claude/projects/-Users-kichan-Desktop-studies-local-ai/memory/project_local_ai_assistant.md`

- [ ] **Step 1: 전체 단위 테스트 + 앱 빌드**

Run: `./gradlew testDebugUnitTest :app:assembleDebug`
Expected: 전 모듈 PASS, BUILD SUCCESSFUL.

- [ ] **Step 2: 앱 설치 + 디바이스 smoke**

Run: `./gradlew :app:assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk`
(connectedAndroidTest로 모델이 지워졌으면 onboarding 재다운로드 또는 `/tmp/model-check` 백업 push.)
수동:
1. 채팅 → 음성 토글 ON → 메시지 전송 → 응답 스트리밍 종료 후 한국어 낭독.
2. 낭독 중 새 메시지 → 이전 낭독 끊김.
3. 토글 OFF → 낭독 안 함.
4. 화면 나갔다 복귀 → 잔여 낭독 없음.

- [ ] **Step 3: memory 갱신**

`project_local_ai_assistant.md`에 v0.4.A(TTS 음성 응답) 완료 기록. STT 입력 + TTS 출력으로 핸즈프리 대화 기반 완성.

- [ ] **Step 4: push + PR**

> 본 브랜치는 v0.4(PR #11) 위에 쌓임. PR #11이 먼저 master로 머지되면, 이 브랜치를 master에 rebase/merge해 TTS 변경만 남도록 한 뒤 PR. (또는 base를 `feature/v0.4-plan`으로 스택 PR.) 머지 순서는 사용자와 확인.

```bash
git push -u origin feature/v0.4a-tts
# PR #11 머지 후:
gh pr create --base master --head feature/v0.4a-tts \
  --title "v0.4.A: TTS 음성 응답 (채팅 답변 낭독)" \
  --body "Android TextToSpeech로 어시스턴트 응답 낭독. 상단바 토글(기본 OFF), 스트리밍 완료 시 낭독. STT 입력 + TTS 출력으로 핸즈프리 대화 기반."
```

---

## Self-Review (작성자 체크)

- **Spec coverage:** TtsSpeaker(:local)+@Provides(T1), feature_chat :local 의존+ChatViewModel 통합+테스트(T2), ChatScreen 토글+strings(T3), 빌드/smoke/memory/PR(T4). spec 컴포넌트 전부 매핑.
- **Placeholder scan:** 모든 코드 스텝 실제 코드. "기존 그대로" 표기는 변경 지점 명확히 하기 위한 컨텍스트 앵커(주변 코드는 plan에 전체 인용됨 — ChatViewModel 현재 본문은 spec/plan 작성 시 확인됨).
- **Type consistency:** `TtsSpeaker.speak(text)/stop()`, `ChatViewModel(startChatSession, tts)`, `ChatState.ttsEnabled`, `onToggleTts()` 전 태스크 일관. 기존 테스트 4개 생성자 2-인자로 갱신 명시.

## 미해결 가정
- 낭독은 collect 정상 완료 시에만(취소=CancellationException, 에러=catch → speak 미도달). 의도된 동작.
- TextToSpeech 한국어 음성 데이터가 기기에 있어야 실제 소리. 없으면 무음(no-op) — smoke에서 확인.
