# v1.0-about — About·라이선스·프라이버시 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Settings에서 진입하는 About 화면(앱 버전 + OSS 라이선스 + 프라이버시 정책)을 추가한다.

**Architecture:** `feature_settings`를 내부 NavDisplay(Settings↔About)로 만들고 정적 `AboutScreen`을 추가. 버전은 `:app`의 `BuildConfig.VERSION_NAME`을 `SettingsScene`에 주입. 라이선스는 수기 큐레이션(전부 Apache-2.0) + Gemma 모델 별도 약관, 프라이버시는 앱내 텍스트(한·영).

**Tech Stack:** Compose, Navigation3(feature 내부), Android resources.

**선행:** v1.0-i18n master. spec: `docs/superpowers/specs/2026-06-09-v1.0-about-design.md`. 브랜치 `feature/v1.0-about`.

**확인된 현황:**
- `SettingsScene(onBack)` → `SettingsScreen(onBack, viewModel)`(브리핑 토글, onBack 미사용). `feature_settings`에 nav3/serialization 의존 **없음**(추가 필요).
- 서브라우트 패턴: `feature_memo/MemoSubRoute`(sealed NavKey, @Serializable) + `MemoScene`의 `rememberNavBackStack`+`NavDisplay`+`entryProvider`.
- `:app` `AssistantApp.kt:97` `SettingsScene(onBack = { backStack.removeLastOrNull() })`. `:app` `buildConfig=true` → `com.just.assistant.BuildConfig.VERSION_NAME` 사용 가능.
- 라이선스 표기: 목록 + "Apache License 2.0" + URL(전문 임베드는 비범위). Gemma 모델은 Gemma Terms of Use 별도.

---

## File Structure
- Modify: `feature_settings/build.gradle.kts` (nav3 + serialization)
- Create: `feature_settings/src/main/java/com/just/feature/settings/SettingsSubRoute.kt`
- Modify: `feature_settings/src/main/java/com/just/feature/settings/SettingsScene.kt`
- Modify: `feature_settings/.../settings/SettingsScreen.kt`
- Create: `feature_settings/.../settings/AboutScreen.kt`
- Modify: `feature_settings/src/main/res/values/strings.xml`, Create: `.../values-en/strings.xml` 갱신(About strings)
- Modify: `app/src/main/java/com/just/assistant/AssistantApp.kt`

---

## Task 1: feature_settings 내부 네비 의존성 + 서브라우트 + Scene

**Files:**
- Modify: `feature_settings/build.gradle.kts`
- Create: `feature_settings/src/main/java/com/just/feature/settings/SettingsSubRoute.kt`
- Modify: `feature_settings/src/main/java/com/just/feature/settings/SettingsScene.kt`
- Modify: `feature_settings/src/main/java/com/just/feature/settings/settings/SettingsScreen.kt`

- [ ] **Step 1: build.gradle 의존성 추가**

`feature_settings/build.gradle.kts`의 `plugins`에 serialization 추가(없으면):
```kotlin
    alias(libs.plugins.kotlin.serialization)
```
dependencies에 추가(feature_memo와 동일):
```kotlin
    implementation(libs.androidx.navigation3.runtime)
    implementation(libs.androidx.navigation3.ui)
    implementation(libs.androidx.lifecycle.viewmodel.navigation3)
    implementation(libs.kotlin.serialization)
```

- [ ] **Step 2: SettingsSubRoute**

`SettingsSubRoute.kt`:
```kotlin
package com.just.feature.settings

import androidx.navigation3.runtime.NavKey
import kotlinx.serialization.Serializable

internal sealed interface SettingsSubRoute : NavKey {
    @Serializable
    data object List : SettingsSubRoute

    @Serializable
    data object About : SettingsSubRoute
}
```

- [ ] **Step 3: SettingsScene → 내부 NavDisplay + appVersion**

`SettingsScene.kt`:
```kotlin
package com.just.feature.settings

import androidx.compose.runtime.Composable
import androidx.navigation3.runtime.entryProvider
import androidx.navigation3.runtime.rememberNavBackStack
import androidx.navigation3.ui.NavDisplay
import com.just.feature.settings.settings.AboutScreen
import com.just.feature.settings.settings.SettingsScreen

@Composable
fun SettingsScene(onBack: () -> Unit, appVersion: String) {
    val backStack = rememberNavBackStack(SettingsSubRoute.List)
    NavDisplay(
        backStack = backStack,
        entryProvider =
            entryProvider {
                entry<SettingsSubRoute.List> {
                    SettingsScreen(
                        onBack = onBack,
                        onOpenAbout = { backStack.add(SettingsSubRoute.About) },
                    )
                }
                entry<SettingsSubRoute.About> {
                    AboutScreen(
                        appVersion = appVersion,
                        onBack = { backStack.removeLastOrNull() },
                    )
                }
            },
    )
}
```

- [ ] **Step 4: SettingsScreen에 About row**

`SettingsScreen.kt`의 시그니처에 `onOpenAbout` 추가, 토글 아래 클릭 row 추가. import 추가: `androidx.compose.foundation.clickable`, `androidx.compose.foundation.layout.Spacer`, `androidx.compose.foundation.layout.height`.
```kotlin
internal fun SettingsScreen(
    onBack: () -> Unit,
    onOpenAbout: () -> Unit = {},
    viewModel: SettingsViewModel = hiltViewModel(),
) {
```
`Column(...) { Row(브리핑 토글) ... }` 안, 브리핑 Row 아래에 추가:
```kotlin
                Spacer(Modifier.height(8.dp))
                Row(
                    modifier = Modifier
                        .fillMaxWidth()
                        .clickable(onClick = onOpenAbout)
                        .padding(vertical = 12.dp),
                    verticalAlignment = Alignment.CenterVertically,
                ) {
                    Text(
                        stringResource(R.string.about_open_label),
                        style = MaterialTheme.typography.titleMedium,
                        modifier = Modifier.weight(1f),
                    )
                }
```

- [ ] **Step 5: 컴파일**

Run: `./gradlew :feature_settings:compileDebugKotlin`
Expected: FAIL (AboutScreen·about_open_label 미존재) — Task 2/3에서 추가. (또는 임시로 진행: Task 2~3 후 컴파일.)
> 이 Task는 Task 2(strings)·실제 AboutScreen(Task 2 통합) 완료 후 함께 컴파일된다. 분리 커밋이 어려우면 Task 1·2를 한 커밋으로 합쳐도 됨.

- [ ] **Step 6: (Task 2와 함께 커밋)**

---

## Task 2: strings (About, 한·영) + AboutScreen

**Files:**
- Modify: `feature_settings/src/main/res/values/strings.xml`
- Modify: `feature_settings/src/main/res/values-en/strings.xml`
- Create: `feature_settings/src/main/java/com/just/feature/settings/settings/AboutScreen.kt`

- [ ] **Step 1: values/strings.xml (한국어) About 추가**

`feature_settings/src/main/res/values/strings.xml`의 `</resources>` 앞에 추가:
```xml
    <string name="about_open_label">앱 정보</string>
    <string name="about_title">앱 정보</string>
    <string name="about_version">버전 %1$s</string>
    <string name="about_licenses_header">오픈소스 라이선스</string>
    <string name="about_licenses_intro">이 앱은 다음 오픈소스 라이브러리를 사용하며, 모두 Apache License 2.0 하에 배포됩니다.</string>
    <string name="about_licenses_list">• AndroidX / Jetpack Compose\n• Hilt (Dagger)\n• Kotlin Coroutines · Serialization (JetBrains)\n• Coil\n• Room\n• OkHttp · Retrofit\n• AndroidX Navigation3 · Lifecycle · DataStore · WorkManager\n• Google AI Edge LiteRT-LM</string>
    <string name="about_apache_notice">Apache License 2.0\nhttps://www.apache.org/licenses/LICENSE-2.0</string>
    <string name="about_model_header">AI 모델</string>
    <string name="about_model_body">Gemma 4 (E2B) © Google. Gemma 이용약관(Gemma Terms of Use)이 적용됩니다.</string>
    <string name="about_model_terms_url">https://ai.google.dev/gemma/terms</string>
    <string name="about_privacy_header">프라이버시 정책</string>
    <string name="about_privacy_body">이 앱은 사용자의 프라이버시를 최우선으로 설계되었습니다.\n\n• 온디바이스 처리: 입력한 텍스트·이미지·음성과 생성된 메모·일정은 모두 기기 안에서만 처리·저장되며 외부 서버로 전송되지 않습니다.\n• AI 추론: 전적으로 기기에서 실행됩니다. AI 모델 파일은 최초 1회 다운로드되며, 이때 전송되는 것은 모델 파일뿐입니다(사용자 데이터 아님).\n• 음성 인식: 기기의 시스템 음성 인식을 사용하며 해당 처리는 시스템 정책을 따릅니다.\n• 권한: 카메라(사진), 마이크(음성 입력), 캘린더(일정 등록·수정), 알림(알람·아침 브리핑), 정확한 알람에만 사용됩니다.\n• 보관·삭제: 모든 데이터는 기기 내부 저장소에 보관되며 앱 삭제 시 함께 제거됩니다.\n• 추적·광고 없음: 사용자 추적·광고·외부 분석을 수행하지 않습니다.</string>
```

- [ ] **Step 2: values-en/strings.xml (영어) About 추가**

`feature_settings/src/main/res/values-en/strings.xml`의 `</resources>` 앞에 추가:
```xml
    <string name="about_open_label">About</string>
    <string name="about_title">About</string>
    <string name="about_version">Version %1$s</string>
    <string name="about_licenses_header">Open-source licenses</string>
    <string name="about_licenses_intro">This app uses the following open-source libraries, all licensed under the Apache License 2.0.</string>
    <string name="about_licenses_list">• AndroidX / Jetpack Compose\n• Hilt (Dagger)\n• Kotlin Coroutines · Serialization (JetBrains)\n• Coil\n• Room\n• OkHttp · Retrofit\n• AndroidX Navigation3 · Lifecycle · DataStore · WorkManager\n• Google AI Edge LiteRT-LM</string>
    <string name="about_apache_notice">Apache License 2.0\nhttps://www.apache.org/licenses/LICENSE-2.0</string>
    <string name="about_model_header">AI model</string>
    <string name="about_model_body">Gemma 4 (E2B) © Google. Subject to the Gemma Terms of Use.</string>
    <string name="about_model_terms_url">https://ai.google.dev/gemma/terms</string>
    <string name="about_privacy_header">Privacy policy</string>
    <string name="about_privacy_body">This app is designed with your privacy first.\n\n• On-device processing: your text, image, and voice input and the notes/events created from them are processed and stored only on your device, and are never sent to external servers.\n• AI inference: runs entirely on your device. The AI model file is downloaded once; only the model file is transferred at that time (not your data).\n• Speech recognition: uses your device\'s system speech recognition, subject to system policies.\n• Permissions: used only for camera (photos), microphone (voice input), calendar (creating/editing events), notifications (alarms and morning briefing), and exact alarms.\n• Storage and deletion: all data is kept in the app\'s on-device storage and is removed when you uninstall the app.\n• No tracking or ads: the app performs no user tracking, advertising, or third-party analytics.</string>
```

- [ ] **Step 3: AboutScreen**

`feature_settings/src/main/java/com/just/feature/settings/settings/AboutScreen.kt`:
```kotlin
package com.just.feature.settings.settings

import androidx.compose.foundation.layout.Arrangement
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.rememberScrollState
import androidx.compose.foundation.verticalScroll
import androidx.compose.material3.ExperimentalMaterial3Api
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Scaffold
import androidx.compose.material3.Text
import androidx.compose.material3.TopAppBar
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.res.stringResource
import androidx.compose.ui.unit.dp
import com.just.feature.settings.R

@OptIn(ExperimentalMaterial3Api::class)
@Composable
internal fun AboutScreen(
    appVersion: String,
    onBack: () -> Unit,
) {
    Scaffold(topBar = { TopAppBar(title = { Text(stringResource(R.string.about_title)) }) }) { padding ->
        Column(
            modifier = Modifier
                .padding(padding)
                .fillMaxSize()
                .verticalScroll(rememberScrollState())
                .padding(16.dp),
            verticalArrangement = Arrangement.spacedBy(8.dp),
        ) {
            Text(stringResource(R.string.about_version, appVersion), style = MaterialTheme.typography.titleMedium)

            Text(stringResource(R.string.about_licenses_header), style = MaterialTheme.typography.titleSmall)
            Text(stringResource(R.string.about_licenses_intro), style = MaterialTheme.typography.bodyMedium)
            Text(stringResource(R.string.about_licenses_list), style = MaterialTheme.typography.bodyMedium)
            Text(stringResource(R.string.about_apache_notice), style = MaterialTheme.typography.bodySmall)

            Text(stringResource(R.string.about_model_header), style = MaterialTheme.typography.titleSmall)
            Text(stringResource(R.string.about_model_body), style = MaterialTheme.typography.bodyMedium)
            Text(stringResource(R.string.about_model_terms_url), style = MaterialTheme.typography.bodySmall)

            Text(stringResource(R.string.about_privacy_header), style = MaterialTheme.typography.titleSmall)
            Text(stringResource(R.string.about_privacy_body), style = MaterialTheme.typography.bodyMedium)
        }
    }
}
```
> `onBack`은 시그니처에 유지(뒤로 제스처/향후 back 버튼용). 시스템 뒤로 가기로 List 복귀(NavDisplay backStack). MVP에선 TopAppBar 별도 back 버튼 생략 가능.

- [ ] **Step 4: 컴파일 + parity**

Run: `./gradlew :feature_settings:compileDebugKotlin :feature_settings:assembleDebug`
Expected: BUILD SUCCESSFUL.
name parity 확인:
```bash
diff <(grep -oE 'name="[^"]+"' feature_settings/src/main/res/values/strings.xml | sort) \
     <(grep -oE 'name="[^"]+"' feature_settings/src/main/res/values-en/strings.xml | sort) && echo PARITY_OK
```
Expected: `PARITY_OK`.

- [ ] **Step 5: Commit (Task 1+2 함께)**

```bash
git add feature_settings/build.gradle.kts feature_settings/src/main
git commit -m "feat(about): feature_settings 내부 네비 + AboutScreen(버전·라이선스·프라이버시) 한·영"
```

---

## Task 3: `:app` 버전 주입 + 빌드 + smoke + memory + PR

**Files:**
- Modify: `app/src/main/java/com/just/assistant/AssistantApp.kt`
- Modify: `/Users/kichan/.claude/projects/-Users-kichan-Desktop-studies-local-ai/memory/project_local_ai_assistant.md`

- [ ] **Step 1: SettingsScene에 appVersion 전달**

`AssistantApp.kt`:
- import 추가: `import com.just.assistant.BuildConfig`
- `entry<AssistantRoute.Settings>` 의 `SettingsScene(onBack = { backStack.removeLastOrNull() })` 를:
```kotlin
                        entry<AssistantRoute.Settings> {
                            SettingsScene(
                                onBack = { backStack.removeLastOrNull() },
                                appVersion = BuildConfig.VERSION_NAME,
                            )
                        }
```

- [ ] **Step 2: 전체 빌드 + 테스트**

Run: `./gradlew testDebugUnitTest :app:assembleDebug`
Expected: 전 모듈 PASS, BUILD SUCCESSFUL. (기존 `SettingsViewModelTest`는 VM 변경 없어 무영향.)

- [ ] **Step 3: 디바이스 smoke**

Run: `./gradlew :app:assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk`
수동: Capture → 설정 → "앱 정보" → 버전(0.1.0)·라이선스 목록·Gemma 약관·프라이버시 표시 확인. 영어 로케일에서 영어 표시 확인.

- [ ] **Step 4: memory 갱신**

`project_local_ai_assistant.md`에 v1.0-about(About·라이선스·프라이버시) 기록. 라이선스=목록+Apache-2.0 URL, 모델=Gemma Terms, 프라이버시=앱내 한·영.

- [ ] **Step 5: push + PR**

```bash
git add app/src/main/java/com/just/assistant/AssistantApp.kt
git commit -m "feat(app): SettingsScene에 앱 버전 주입(About 화면)"
git push -u origin feature/v1.0-about
gh pr create --base master --head feature/v1.0-about \
  --title "v1.0-about: About·라이선스·프라이버시" \
  --body "Settings 내부 About 화면 — 앱 버전, OSS 라이선스(전부 Apache-2.0 목록+URL), Gemma 모델 약관, 온디바이스 프라이버시 정책(앱내 한·영). 출시 준비 sub-project 2."
```

---

## Self-Review (작성자 체크)
- **Spec coverage:** 내부 네비+SubRoute+Scene(T1), About strings 한·영+AboutScreen(T2), 버전 주입+빌드/smoke/memory/PR(T3). Gemma 약관 별도 표기·프라이버시 앱내 텍스트·parity 모두 포함.
- **Placeholder scan:** 모든 strings 전문(한·영) 포함. Apache-2.0 전문 임베드는 의도적 비범위(목록+URL) — 사용자에게 고지됨.
- **Type consistency:** `SettingsScene(onBack, appVersion)`, `SettingsScreen(onBack, onOpenAbout)`, `AboutScreen(appVersion, onBack)`, `SettingsSubRoute.List/About`, string name 한·영 동일. `:app`가 `BuildConfig.VERSION_NAME` 전달.

## 미해결 가정(구현 중 확인)
- `feature_settings/build.gradle.kts`에 serialization 플러그인 alias 정확명(`libs.plugins.kotlin.serialization`) — feature_memo와 동일 확인.
- Task 1·2는 상호의존(AboutScreen/strings) → 한 커밋으로 묶음(분리 시 Task 1 컴파일 실패 정상).
- `SettingsScreen`의 기존 `onBack` 미사용 유지(컴파일 OK). About 진입 row만 추가.
